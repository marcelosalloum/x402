# API server URL (start the server first: cd ../server && pnpm dev)
VITE_API_TARGET=http://localhost:4021

# Networks to query (comma-separated, must be supported networks)
VITE_NETWORKS=base-sepolia,stellar-testnet
